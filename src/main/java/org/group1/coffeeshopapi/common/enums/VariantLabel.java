package org.group1.coffeeshopapi.common.enums;

// The fixed set of names a product variant can have (e.g. a Medium/Large drink, or a per-piece
// item). A closed set rather than free text, so labels stay consistent across admins.
public enum VariantLabel {
    MEDIUM, LARGE, PIECE
}
